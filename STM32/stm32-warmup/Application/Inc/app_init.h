/**
 * app_init.h — Public entry point for the warmup application.
 *
 * CubeMX generates freertos.c with a function MX_FREERTOS_Init().
 * You add ONE line at the end of that function:
 *
 *     warmup_app_init();
 *
 * That's the only modification to CubeMX-generated code.
 */
#ifndef APP_INIT_H
#define APP_INIT_H

void warmup_app_init(void);

#endif /* APP_INIT_H */
